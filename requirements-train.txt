# Training, evaluation and ONNX export: train_model.py, evaluate.py, export_onnx.py.
#
# Install in a SEPARATE environment from requirements.txt (see the note there).
# Use Python 3.11 or 3.12 — neither TensorFlow nor MediaPipe ships wheels for 3.13+.
#
# On Linux the [and-cuda] extra pulls the CUDA and cuDNN wheels, so an NVIDIA driver is
# the only system dependency. On Windows this installs the CPU build: TensorFlow dropped
# native GPU support after 2.10, which is why training this project on Windows is
# CPU-only and why the Linux side of a dual boot is worth using.
tensorflow[and-cuda]>=2.16; sys_platform == "linux"
tensorflow>=2.16; sys_platform != "linux"

tf2onnx
onnx
onnxruntime

scikit-learn
matplotlib

# preprocessing.render_skeleton runs inside the tf.data pipeline.
opencv-python
numpy
